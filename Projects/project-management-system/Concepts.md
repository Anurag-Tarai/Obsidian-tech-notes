```java
@AllArgsConstructor: Generates a constructor with parameters for all fields.

@NoArgsConstructor: Generates a no-argument (default) constructor.

@JsonIgnore: Excludes the marked field from being serialized into JSON, often used to hide sensitive or unnecessary data.
```